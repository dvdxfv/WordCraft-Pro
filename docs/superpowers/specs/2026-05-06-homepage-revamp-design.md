# 落地页整改设计文档

**日期**：2026-05-06  
**范围**：`web/wordcraft_landing.html`  
**目标**：将落地页从"工具展示页"升级为"付费产品页"，同步品牌文案（豆包→DeepSeek-V4），新增定价区块，Footer 补充急诊入口。

---

## 1. 文案替换（豆包 → DeepSeek-V4）

| 文件行 | 原文 | 改后 |
|--------|------|------|
| 第655行（Hero badge） | `全新 v2.0 · AI 智能排版引擎` | `DeepSeek-V4 驱动 · 毕业季限时方案已上线` |
| 第658行（Hero sub） | `...内置豆包大模型 AI 辅助。` | `...由 DeepSeek-V4 提供 AI 能力，单次检查成本低于 ¥0.002。` |
| 第740行（AI辅助功能卡） | `内置豆包大模型，支持排版规则生成...` | `接入 DeepSeek-V4，支持排版规则生成...` |

---

## 2. 导航栏

在现有导航项（功能 / 流程 / 模板 / 质量检查）末尾新增：

```html
<li><a href="#pricing" class="nav-link">定价</a></li>
```

---

## 3. 定价区块（新增 `#pricing` section）

**插入位置**：`#qa` 区块之后，`#download` CTA 之前。

**背景**：`var(--surface)`（与 `#qa` 交替一致）。

### Section 文案

- Tag：`定价方案`
- Title：`选择适合你的方案`
- Sub：`毕业季限时优惠，Pro 年付直降 ¥10`

### 三列定价卡

#### 免费版（¥0）

功能列表：
- 文档上传预览（.docx / .pdf / .xlsx）
- 规则层检查无限次（错别字 / 标点 / 一致性）
- AI 深度检查 3 次 / 月

CTA 按钮：`即将开放`，`disabled`，`cursor: not-allowed`，灰色。

#### 论文急诊包（¥49 / 次）⭐ 推荐

视觉：白色描边 + "推荐" badge。

功能列表：
- AI 全文深度检测格式问题
- 出具完整格式检查报告
- 经确认后人工协助修改（一次修改闭环）
- 24h 内交付结果

适用场景说明：适合不想订阅、急用一次的毕业生用户。

CTA 按钮：`即将开放`，`disabled`，`cursor: not-allowed`，灰色。

#### Pro（月付 + 年付）

价格展示：
- `¥9.9 / 月`
- `~~¥79~~ ¥69 / 年` + "毕业季限时" 红点 badge

功能列表：
- AI 深度检查无限次
- 格式急诊报告
- 个人规则库（保存 / 复用）
- 深度交叉引用（参考文献 / 图表链接）
- 优先技术支持

CTA 按钮：`即将开放`，`disabled`，`cursor: not-allowed`，灰色。

---

## 4. 底部 CTA 区块（`#download`）更新

### 文案

```
立即开始使用 WordCraft Pro
规则检查永久免费，AI 深度检查按需付费，论文季专属优惠进行中。
```

### 按钮

| 按钮 | 行为 |
|------|------|
| 查看使用文档 | 保持原样不动 |
| 查看定价方案（新增） | 平滑滚动到 `#pricing` |

两个按钮并排显示。

---

## 5. Footer 更新

### 链接区新增两项

| 链接文字 | 行为 |
|----------|------|
| 论文格式急诊 | 平滑滚动到 `#pricing` 的急诊包卡片（锚点 `id="pricing-emergency"`） |
| 📱 微信联系（敬请期待） | 灰色禁用，`cursor: not-allowed`，hover tooltip：`即将上线` |

原有链接（README / GitHub / 使用文档）保持不变，新增项追加在末尾。

---

## 6. 不在此次范围内

- 支付系统接入（所有 CTA 按钮均为禁用占位）
- 微信二维码图片（等备好后替换占位文字）
- 使用文档页面（`查看使用文档` 按钮行为不变，文档本身不在此次开发范围）
- 竞品对比表 / 用户信任信号（等有真实数据再加）
- `web/index.html` 主应用页面（不在此次范围）

---

## 7. 实现约束

- 无构建工具，直接编辑 `wordcraft_landing.html` 内联 `<style>` 和 `<script>`
- 新增 CSS 类追加到现有 `<style>` 块末尾，不重构已有样式
- 定价卡样式跟随现有 `.feat-card` 风格（border / border-radius / transition），保持视觉一致
- 所有禁用按钮统一用 `pointer-events: none; opacity: 0.4` 实现，不用 JS
